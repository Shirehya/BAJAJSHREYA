# Application Properties
spring.application.name=bajaj-finserv-challenge

# Logging configuration
logging.level.com.bajajfinserv.challenge=INFO
logging.level.org.springframework.web.client.RestTemplate=DEBUG

# Server configuration (not needed for this challenge but good practice)
server.port=8080

# HTTP client configuration
spring.http.client.timeout.connection=30000
spring.http.client.timeout.read=30000